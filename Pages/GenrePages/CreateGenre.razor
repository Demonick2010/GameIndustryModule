@*Урок 10 (5) ********************************@
@page "/genres/create"

<div class="row">
    <div class="col">
        <h1 class="text-info">Create Genre</h1>
    </div>
</div>
<div class="row">
    <div class="col-6">
        @*Урок 11 (2.1)*@
        @*Создаём форму, но, для неё нужна модель*@
        @*<EditForm Model="genre" OnValidSubmit="Create">*@
            @*Урок 11 (6)*@
            @*<DataAnnotationsValidator />
            <div class="form-group">
                <label>Genre Name: </label>
                <div>
                    <InputText class="form-control" @bind-Value="genre.Name" />
                    <ValidationMessage For="(() => genre.Name)" />
                </div>
            </div>
            <button class="btn btn-primary">Save</button>
        </EditForm>*@
        <FormForGenre  Genre="genre" OnValidSubmit="Create"/>
    </div>
    <div class="col-6">

    </div>
</div>

@code {
    // Урок 11 (2.2)
    // Назначаем модель для формы
    Genre genre = new();

    void Create()
    {
        Console.WriteLine(genre.Name);
    }
}
@*********************************************@